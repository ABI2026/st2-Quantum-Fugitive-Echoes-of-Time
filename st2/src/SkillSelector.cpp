#include "SkillSelector.h"
/*
SkillSelector::SkillSelector() {
	if (!m_texture.loadFromFile("Resources/Images/SkillSelector"))
		LOG_ERROR("failed loading SkillSelector texture");
}
void SkillSelector::update(std::shared_ptr<Eventsystem>& eventsystem, std::shared_ptr<Soundsystem>& soundsystem, double deltatime) {

 }
void SkillSelector::draw(sf::RenderWindow& window) {
	sf::Sprite m_sprite(m_texture);
	m_sprite.setOrigin({ m_texture.getSize().x /2,m_texture.getSize().y / 2 });
	m_sprite.setPosition({ 1920 / 2,1080 / 2 });
	window.draw(m_sprite);
}*/
